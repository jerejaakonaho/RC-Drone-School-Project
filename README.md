# RC-Drone-School-Project

A repo containing all the code for a RC Drone University course using Micropython as the language.

# Instructions
1. Open terminal and clone the repo:
2. git clone https://github.com/jerejaakonaho/RC-Drone-School-Project
3. cd RC-DRONE-School-Project
4. Create virtual environment: venv\Scripts\activate (windows)
5. Install dependencies: pip install -r requirements.txt
WIFI_SSID = '[Wifi name]'
WIFI_PASSWORD = '[Wifi password]
6. MicroPython editor to transfer the code to the microcontroller. **Create a secrets.py** that contains WIFI_SSID = 'wifiname' and WIFI_PASSWORD = 'wifipassword'
Copy the main.py and save it to the Pico W.
7. Run main.py using the play button in editor
8. to be continued